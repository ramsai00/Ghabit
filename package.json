{
  "name": "ghabit",
  "version": "0.1.0",
  "description": "Ghabit habit tracker with an Express + SQLite backend",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "copy:web": "mkdir -p www && cp index.html styles.css app.js www/",
    "prepare": "npm run copy:web && npx cap sync",
    "android": "npm run copy:web && npx cap copy android && npx cap open android"
  },
  "dependencies": {
    "@capacitor/core": "^6.0.0",
    "express": "^4.18.4",
    "sqlite3": "^5.1.6"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0",
    "@capacitor/android": "^6.0.0"
  }
}
